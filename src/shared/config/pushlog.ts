/** Тип упражнения по умолчанию (MVP — отжимания). */
export const DEFAULT_EXERCISE_TYPE_ID = "exercise.pushups" as const;

/** Быстрые кнопки на главном экране (повторений за подход). */
export const QUICK_ADD_PRESETS = [10, 20] as const;
